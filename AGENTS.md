# Aster Agent 规则

## 项目目标与状态

Aster 是两人协作的智能体客服学习项目，目标是通过调研、决策、小步实现和持续验收形成可理解、可维护的系统。当前只有 ADR 明确标为 accepted 的内容属于已接受决策；其他架构文字仍是建议或假设。

## 阅读顺序

每次工作依次阅读：

1. 本文件；
2. `PLAN.md`；
3. `docs/requirements.md`、当前任务相关架构文档和 ADR；
4. `DEVLOG.md` 最近记录；
5. 当前代码、测试和 Git 状态。

## 标准工作流

开始里程碑前说明目标、范围、非目标、预计文件、验收标准、风险与假设。一次只推进一个明确里程碑；实现后执行必要检查，查看完整 diff，确认没有无关修改，更新 `PLAN.md` 和 `DEVLOG.md`，汇报已验证与未验证内容，然后停止。

## 实现与质量

- 优先完成小而真实的纵向链路；每个新文件必须服务当前验收标准。
- 代码优先清晰；不要预建空模块、万能 Service/Router/Utils、无真实变化点的接口或工厂。
- Aster Core 不得直接依赖具体消息平台 SDK。第三方 SDK 只出现在经确认的边界。
- 第三方依赖一律安装到项目本地 `.venv`（依赖声明见 `pyproject.toml`），禁止安装进用户全局 Python 或 conda 环境。
- 提交前必须通过完整质量门：`make check`（ruff 检查与格式、mypy 类型、全部离线测试）；只有实际执行过才能声称通过。
- Agent、RAG、工具、渠道和客服业务保持可解释边界，但不要为未来需求制造框架。
- 不提交密钥、Token、真实用户数据；不使用宽泛异常捕获掩盖错误。
- 测试重要行为和边界，不追求覆盖率数字；只有实际执行后才能声称检查通过。

## 决策与记录

- 文档使用：已确认需求、当前建议、已接受决策、待验证假设、未来可能方向。
- 高成本、跨模块、难回退或长期约束技术选择的决策，经人工确认后才写 ADR。ADR 至少写背景、选项、结果、理由、代价和重评条件。
- 未决定事项留在 `PLAN.md` 的决策清单中；可逆低风险默认值记录为假设。
- `DEVLOG.md` 只记录目标、完成内容、关键发现、验证命令、遗留问题和下一步，不粘贴终端流水账。

## Git 修改边界

保留用户已有修改，不重构当前里程碑之外的文件，不主动 push、force push、改远端或重写历史。提交前检查状态和 diff；发现无法绕开的重叠修改时停止并说明。

更具体的一次任务流程见 `docs/agent-workflow.md`。
