# 参与 Aster

Aster 当前以学习和可维护性优先。一次贡献应足够小，让另一位成员能独立理解、运行和验收。

## 开始前

按顺序阅读 `README.md`、`docs/requirements.md`、`PLAN.md`、相关架构文档/ADR，以及 `DEVLOG.md` 最近记录。Coding Agent 还需阅读 `AGENTS.md`。

从 `PLAN.md` 的当前里程碑认领一项任务；不要提前实现“后续候选里程碑”。开始前在任务或 PR 描述中写清目标、范围、非目标、预计文件、验收标准和假设。

## 分支与 PR

在仓库建立有效 Git 基线后：

1. 从团队约定的主分支更新本地分支；
2. 为一个里程碑或更小任务创建短期分支；
3. 提交聚焦、可审查的 commit；
4. PR 描述列出实际改动、验证命令、未验证内容和待决定问题；
5. 至少由另一位成员确认验收标准后合并。

两人团队暂不引入复杂审批层级。不要 force push 共享分支，也不要让格式化或重构淹没功能改动。

## 提交前检查

- 运行完整质量门：`make check`（ruff 检查与格式、mypy 类型检查、全部离线测试；CI 执行同一组命令）；
- 查看完整 `git status` 和 diff，移除无关修改；
- 确认没有密钥、Token、真实用户数据或平台凭据；
- 更新 `PLAN.md` 的任务状态和 `DEVLOG.md` 的高信号记录；
- 只报告实际执行过的验证，无法执行时说明原因与影响。

## 架构问题

可逆的小选择可以先采用简单默认值，并记录假设。涉及技术栈、持久化边界、进程拆分、公共协议、许可证或长期迁移成本的选择，先写候选与权衡，等待人工确认；确认后再创建 ADR。没有接受结果时不要创建“已决定”的 ADR。
